# frozen_string_literal: true

# requirements: two people can play on the board, displays the board inbetween turns
# only using terminal

# Board class with function to display
# user input with numbers 1-9, 1 is upper left and 9 is bottom right. Number goes right then down.
# so 2d array

# i don't really get what instance variables there would be here since it's just one board

class Board
  def initialize

  end
end
